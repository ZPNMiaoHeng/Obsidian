10. 反压处理方式、握手方式、信用设置点；
	1. Round Trip
	2. ![[Pasted image 20241022095045.png]]
	3. ![[Pasted image 20241022095008.png]]
11. 数据通路流程图，尽可能用图形化描述；
12. 调度
	- [ ] 学习 QoS 文档；
	- [ ] 学习 LRU：文档，代码；
13. 异步 FIFO 按照 checklist 要求；
14. 时钟树：内部含有衍生时钟，分频处理后的始终才需要列出；
	1. 目的：为 BES 同事提醒对生成时钟约束；
15. 复位树：同时钟树；
	1. 复位信号多驱动，导致时序问题，使用**打拍处理**；
	2. 新要求：软复位，通过在**非敏感列表**（**逻辑处理**）做的处理；
		1. 通过 **与门** 处理逻辑；
		2. ![[Pasted image 20241022100947.png]]
16. 复位流程图：crg 模块设定，同步到设计方案，对异步复位时钟信号处理友好；
	1. ![[Pasted image 20241022101141.png]]
17. IO 单独模块


##### 异步
2. 频点：时钟分频处理后，就出现频点了。
5. 考虑扇出问题，在同步后在跨时钟域处理，复制信号；
	1. ![[Pasted image 20241022110112.png]]

##### 代码
3. IP 厂商标准单元规定，防止出现时序、面积、工艺等等问题；
	1. 如何让两个 cell 摆放的近呢？
		1. 表面上是两个寄存器，其实在 fab 工艺上就一个cell；
		2. 后端使用脚本把他们摆在一起；
4. DFX：将 **IP 输出端口**接到 CSR RO 进行观察；
5. DFX：将 **IP 输入端口**接到 CSR RW 的输出；
	1. 功能仿真阶段：通过 CSR 更改，减少编译时间；
	2. 回片阶段：工艺缺陷影响导致存在部分晶圆失效（比如 MEM 失效），通过配置更改 IP，或许可以抵消这个工艺缺陷，提高流片良率；
6. 在模块内使用 sdc 可以约束为一个单独的时钟；
	1. harden/RLM/BLOCK：都是一个概念，就是DC时使用的模块；
9. 有两种情况下，block 做不了栈入栈出：时沿要求和性能要求
	1. 要求后端 Block 摆放的近一些；
	2. 少量的 Port 后端能解决：换性能更高的 cell；
12. AXI 接口的 IP 不能做栈入栈出，否则会影响性能；
18. generate 和不使用 generate 在综合工具下无区别，但会存在风险，功能验证不好拖信号；--非必要不用；
21. 表项返回数据，若没有查表操作，需要清零；
25. 接口需要频偏消除设计，否则频偏会持续累积，导致时序出现错误；
	1. 使用链路频偏消除、弹性 buffer 频偏消除；
26. 为了避免出现越界行为；
	1. ![[Pasted image 20241022150030.png]]
	2. 第二个可以的，公共代码 RAM 就是这种使用；

##### parameter
1. 存在问题：
	1. 综合方面：parameter 会影响综合的电路，导致后端工作量增加；
		1. 解决方法：可以通过端口输入参数；
		2. ![[Pasted image 20241022151307.png]]
			1. BASE_OFFSET parameter，导致结果位宽不一样，综合电路也是不同的；
			2. 第二个端口会固定大小，综合电路是相同的；
	2. 例化风险：实例化使用者可能忽略默认值，出现未对齐，导致 bug ；---减少风险；
	3. 对 Emacs 连线不友好；
2. --
3. 专项检查，公司自定义使用；

##### csr
3. csr 可能会在模块时钟域下，不同模块时钟域不同；
	1. csr 节点内处理跨时钟域处理，chain-ctrl；
4. ==软件访问直接关联操作==；
	1. 抽象将软件 csr 配置和硬件抽象分开，只通过数据交互；
	2. 防止两者其中相互影响
5. CPU 访问清零，硬件加一，这时候初始值应该为1；
6. TAB 类型寄存器就RAM，进行读写；
	1. csr chain 间接访问：addr 为 tab csr 地址，data 为 tab csr 的操作、访问 tab 的地址等；
7. 防止死锁；
8. 工作在系统时钟上（启动慢、但稳定）；

##### 时钟
4. 规格：
	1. 无毛刺 MUX：两个时钟域都要有；
	2. 有毛刺 MUX：；
	3. 毛刺是不是功能仿真测不出来，只能时序仿真呢？有无工具检测呢？
6. 时钟 gating 使能信号打拍和产生，必须使用配置复位（后门）：防止出问题时，可以通过配置复位解决，不然无法解决； 
7. 同步使用**公共模块**，会有工具特别检查；
	1. ![[Pasted image 20241023094759.png]]
8. 防止会产生死锁。
9. ==gating 跟业务相关==，配置 gating
10. 脉冲 gating后变成了电平信号，变成多个脉冲；
16. 验证时钟是理想情况，功能无法检测。
	1. ![[Pasted image 20241023100247.png]]
##### 复位
1. 同步复位使用条件 begin end标识。异步复位不存在的。
12. 硬复位
	1. por，power on reset，上电复位电路； 
	2. 管脚硬复位，；
13. 顶层CRG模块，硬复位去抖动电路容抖时间符合芯片规格，需要找硬件电路同事确定的；
14. 解复位时间
15. 多系统同时复位会导致电压瞬间变化，损伤电路。

##### 复位异常

##### RAM初始化
1. MEM 初始化是一套独立的逻辑，是需要屏蔽正常业务读写；
	2. 初始化是额外的硬件电路，对 MEM 进行初始化。
3. 防止解复位后直接读表需求；
	1. 配置类 RAM 是协议流水线中表项；
	2. 功能类 RAM 是 FIFO 的 RAM；
##### RAM奇偶/ECC校验
2. MEM 失效和尺寸相关，小规格的 MEM 可以不需要；
3. ；
4. 