- 中断csr 地址最上面；
- axi 计数器；
- snap_en：跨时钟域处理前的快照；
- DFI Timeout；
- 寄存器内数据拼接地址 4 bits 对齐
- ![[Pasted image 20241028143414.png]]
	- 应该是==axi 多 id 掩码==？
- fte 是什么？
- err 需要加；
- ecc 原理：1bit可纠正，2bit可查；
- v200：先有时钟再有复位，因此做成同步不会影响；
- 2564：无时钟情况下，同步复位无法复位了；
	- 注意：crg 超时要比 DDRC 超时长；
	- 使用参考时钟，通过无毛刺寄存器处理完成时钟切换，好处：保证模块都有时钟；
		- 存在问题：高频、低频时钟切换；
- ==硬件自刷新一个流程只会产生一次，不会产生多个脉冲。==
- 