#PS80256 


项目个人复盘总结（模块视角 + 流程视角）

# 一、模块维度复盘（个人负责模块）

## 1. 本模块本次做得好的地方 & 可推广复用价值
（要求：写具体技术、方法、脚本、流程、避坑经验，写清楚能给团队哪里复用，禁止空话）
● 关键难点攻关、协议边界场景补齐
● Bug 预防、边界覆盖、异常容错优化
● 可固化成规范、Checklist、通用流程的点
● 物理可实现经验
● PPA优化经验等


- **MEM 定制预留未使用 bit 位**  
    前期就为未使用的 bit 预留空间，后续规格变动时可避免重新定制 MEM，大幅缩短迭代周期。
    
- **NTB 中 TCAM 寄存器 fan‑in 问题规避**  
    若 TCAM 用寄存器搭建，布局布线时容易出现 fan‑in 过大的时序问题。  
    解决方案：在代码设计阶段通过**寄存器复制**降低 fan‑in，并建议在 Checklist 中增加一项：DC 综合时关注寄存器 fan‑in 大小的检查。
    
- **异常场景系统梳理**  
    NTB 的 doorbell、单播、组播、IOMMU 等复杂场景存在大量中断异常条件。协议思维导图按串行逻辑梳理，但硬件实现部分逻辑为并行，导致多种中断可能同时触发。  
    通过提取并归类所有中断条件，在不改变协议思维导图的前提下，建立了中断优先级顺序，并拉通验证、协议、测试等同事对齐，避免了遗漏和反复。

## 2.本模块本次存在的不足 & 后续改进方案
（要求：不自我否定、不写虚话，写具体遗漏、考虑不周、迭代返工、场景缺失）
每条不足必配套：「下版本落地改进动作」

#### PRE_MC 设计

1. **前期将 NTB 组播窗口计算提前至 PRE_MC，导致延迟从 1‑cycle 变为 2‑cycle。**  
    影响：增加了关键路径延迟。  
    改进方案：将 NT MCG 计算移至 NT 组播路径（该路径有 3‑cycle 空闲），普通组播的 MCG 与不相等判断保留在 `pcie_route` 模块。
    
2. **PRE_MC 和 PRE_ACS 功能较轻，但分属两个模块，导致文档交互多、探针资源浪费。**  
    影响：开发效率低，资源重复。  
    改进方案：合并为一个 `PRE_MC_ACS` 模块，共用一套探针，减少接口文档和调试开销。
    
3. **原设计规避的 MCG 不一致问题最终仅需上报 `int0` 中断，但设计未及时简化。**  
    影响：留下不必要的高成本设计。  
    改进方案：统一决策规则——非必要不增加流水级。
    

#### NTB 模块

1. **NTB 增加 clock gating 方案后，SRIOV、PRE_MC、PRE_ACS 仍共用 NTB CSR 节点，导致 NTB 的配置寄存器无法有效 gating，CSR 节点本身也无法关闭时钟。**  
    影响：资源浪费，功耗优化不彻底。  
    改进方案：下版本为 NTB 单独分配 CSR 节点，SRIOV、PRE_MC、PRE_ACS 分配一个 CSR 节点。
    
2. **NTB 最终延迟为 5‑cycle，其中 NT 单播路径占满延迟，而 NT 组播路径存在 3‑cycle 空闲、Doorbell 存在 2-cycle 空闲。**  
    影响：延迟资源利用不均。  
    改进方案：未来设计中主动规划空闲周期复用，将非关键计算填充到空闲路径中，压缩整体流水线深度。



# 二、全流程视角复盘（站在团队 / 项目整体设计流程）

## 1. 现有流程中做得优秀、值得继续固化 / 深挖的环节
从需求、架构、文档、编码、仿真、联调、上机、问题闭环、跨组协作任意维度说明：
● 哪些流程效率高、质量好、提前规避风险
● 哪些协作方式、评审机制、前置检查值得保留升级

- **SystemVerilog 语法升级**  
    已全面使用 `logic` 替代 `wire`/`reg`，并结合 `always_comb`、`always_ff` 明确设计意图。建议在编码规范中明确推荐此类写法，便于综合检查和代码阅读（参考 [设计前端verilog编码规范](obsidian://open?file=todo%2F%E5%BE%85%E6%95%B4%E7%90%86%E6%96%87%E6%A1%A3%2F%E8%AE%BE%E8%AE%A1%E5%89%8D%E7%AB%AFverilog%E7%BC%96%E7%A0%81%E8%A7%84%E8%8C%83.md)）。
    
- **模块级 DC 综合前置检查**  
    提前在模块级关注 fan‑in、fan‑out 等物理实现指标，减少后端迭代。建议将相关检查项纳入 [checklist](obsidian://open?file=todo%2F%E5%BE%85%E6%95%B4%E7%90%86%E6%96%87%E6%A1%A3%2Fchecklist.md) 并强制执行。

## 2. 现有流程中不合理、卡顿、低效、需要优化的环节
重点关注：
● 前期需求 / 协议对齐是否滞后
● 文档 / 接口 / 模块交互是否信息差
● 问题复盘是否不及时
● 跨组沟通、评审节奏、交付节点是否不合理
● 质量活动内容较多，是否合理

- **全流程文档汇总沟通低效**  
    逐项催促责任人时需逐一沟通，且聊天工具无法判断是否已读，效率低下。  
    改进方向：建议在 Jira 上设置**自动提醒与状态跟踪**（jira 通过 @ 到某人是否可实现？）

- **新需求加入时评审节奏仓促**  
    NTB 新需求代码交付时间紧，场景复杂，评审时仅靠思维导图梳理，具体实现架构分析不足，容易遗漏使用场景。  
    改进方向：需求评审阶段增加架构级分析节点，要求设计人员提前输出关键实现方案，避免在压力下仓促编码。
    
- **需求/协议对齐存在滞后**  
    部分协议细节（如组播窗口约束）在设计后期才暴露，导致返工。  
    改进方向：在协议评审阶段枚举边界场景，提前在详设文档中标注潜在冲突点。



## 3. 个人对团队下一版迭代的建设性建议
（流程、规范、交付、用例、文档、自动化、协作机制均可）
- **引入 AI 辅助**  
    建议团队层面引入 AI 工具，帮助理解旧代码、优化代码格式、增加注释，提高开发效率。


统一提交要求：
1. 禁止流水账，只沉淀可复用、可改进、可落地内容
2. 亮点必须带「推广价值」
3. 问题必须带「具体改进动作」
4. 所有内容聚焦：质量、效率、流程、技术沉淀
5. 截止时间：6.30 前输出


