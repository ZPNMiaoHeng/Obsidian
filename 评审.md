

遗留问题：

1. DS.05.03：CSR 访问超时、失败，返回 code 不相同，用于通知软件；--孔令涛
	1. CSR 访问不存在地址，返回软件失败；-- RM 已存在；
	2. CSR 间接寄存器；
2. CSR 冲突方式考虑实现：SP---苗恒
	1. 逻辑访问优先，一段延迟（超时，计数器翻转）后暂停一拍 CPU 访问；
	2. CPU优先，CPU 访问较慢，在其等待时间逻辑访问；
3. ECC 校验关注延迟问题；--苗恒
4. CIU 性能需求：batch 操作；--苗恒
5. FIFO 深度需要关注，；
6. 多拍表验证断言控制，方便debug；---
7. 汇总表单：区分特殊表、一般表；


确定问题：
1. ECC 1bit 地址记录上报；---记录最新的 ECC 错误，
	1. 寄存器内容全0可写，否则不可写；
	2. 软件读清寄存器；
2. 表初始化：基于模块级 CSR 控制模块内部表的初始化；---方案确定后通知流水线；


table_hash