#card 

# 规格变动
## 起因
1. 当前 FPGA 模块输入时钟为 160 MHz，umctl2 和 fpga_phy 输入时钟为 160 MHz，但 2564 项目布局布线后时序违例 10 ns。fpga_phy 输入时钟最低为 625 MHz，频率比换算后为 156.25 MHz，无法满足布局布线时序要求。
2. 2564 代码更新，仿真环境中代码更换为最新版本代码；
## 变动规格
- fpga_phy 输入时钟为 160 MHz；
	- 通过内部 PLL 分出一个 80 MHz 时钟；
- umctl2 输入时钟为 80 MHz；
	- 包括 AXI、APB 时钟；
