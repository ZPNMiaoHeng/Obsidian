#card 
# 规格
1. **DDR4-1600 颗粒**选取最大延迟 **12-12-12**；
2. POI 频率比是**1:4**，DFI频率比是**1:2**；
	1. PHY 采样为**下降沿**；


# TODOList
## 端口
- [x] dfi_odt 看是否可以使用 FPGA 内部生成逻辑；--可以，odt使用POI端口信号，移植文件；
- [x] dfi_cke；-- 全高
- [x] dfi_parity_in 具体分析：随命令变化；--最终是忽略，让PHY自动生成

## 模块
- [x] ~~PHY 下降沿采样~~；--改为上升沿了

# 架构设计
###### 时钟复位
- PHY 的时钟为 FPGA 外部输入，解复位已完成；
- 等待 DDRC 配置寄存器初始化完成后，再解开 DDRC 复位；
###### 命令转换
- 每两条DFI命令转换为一条POI命令，**为了防止读写命令出现在 POI 协议中 slot1、3**。
	- 拼接两条 DFI 命令；
	- 数据格式转化；
###### 数据采样
- ~~PHY 模块采用下降延采样~~
	- ~~原因：PHY IP 中时钟和复位是同步，当前 FPGA 系统是同步，umctl2 解复位下个DDRC周期会拉高复位信号。~~
		- ~~若后续信号都按照 PHY 时钟上升沿采样，读写命令会出现在slot2、4，PHY 不支持。~~
	- ~~解决方案：使用 PHY 时钟的下降沿采样。~~

- 2024年12月5日20:39:23
	- 经评估，上升沿采样不影响。当前设计是将 DFI 
# POI 信号转换
###### winRank
- 相关信号：mc_CS_n，mcRdCAS、mcWrCAS；
- 转换过程：
	- 定义：只有当 mcRdCAS、mcWrCAS 有效时，此信号数据才有效；
	- mc_CS_n 信号当 ==phase1== 低电平有效，其值就是1，否则为0；
	- ![[60bbaf235f4af457cd2ab9cf8fb96b7b.jpg]]