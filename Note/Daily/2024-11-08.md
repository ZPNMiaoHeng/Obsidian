#todo 
# `ris:Todo`
## 上午
- [?] 非对齐传输：
	- [x] 比如 512 bits 数据，在 axi 上传输 256 bits✖️ 3，通过掩码标记有效位；DFI 对应也是三条传输数据；但 POI 上数据传输位宽为固定值，就要被拆分为2条写命令了。---地址对齐传输！
- [?] ECC 是在哪个模块做的？------控制器IP
	- [x]  axi 传输 256 bits✖️ 2，DFI 数据传输是 288 ✖️ 2。
	- [-] ECC 是每256做一次 32 bits ECC数据，还是 512 得到一个 64 bits ECC数据；
		- [x] 每 64 bits 数据会有对应 8 bits ECC 校验码，可以从波形上观察是 dfi_wrdata_en 是按照 9 bit 为一个块传输。 ![[Pasted image 20241108141129.png]]
- [x] 让王工打开一个 2564 ECC 波形； --宇！！
- [?] dfi_wrdata_en 中每一位代表什么？----wwf：两个 phase；
## 下午
- [x] 构建测试用例：地址对齐；
	- [x] 在 POI 协议中 wrDataEn 是 PHY 发给 MC ，表示 PHY 可以接受数据了。但在 DFI 协议中 dfi_wrdata_en 是 MC 发送给 PHY，表示写数据使能。
		- [x] 设计：数据接受到全部，进入等待 POI wrDataEn（**先来就无法在下个周期给有效数据了**），其有效后传输完成；
		- [x] 验证：外部wrDataEn输入全高，当计数器是2’b10时，卡拍输出；

## 晚上

# `fas:Question`遗留问题
1. 
![[Pasted image 20241108113414.png]]
# `fas:Delicious`

# `ris:Emotion`
