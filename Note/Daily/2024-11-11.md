#todo 
# `ris:Todo`
## 上午
- [x] DFI 中 cs[3:2] 表示：MRS/ZQ/REF，其中 REF 会选择单个 phase 刷新。查看 FPGA 如何控制phase。--- FPGA 中的 cs 位宽为8，只选择使用哪个 slot0；
	- [x] 确定 POI 中 winRank 是否可以使用呢？--选择rank，dfi 好像没有相关配置。
- [x] rank 和 phase 区别；
	- Rank是内存模块中的一个独立存储区域，可以独立地响应内存控制器的命令；
	- Phase通常与内存的时序和同步有关。在DDR内存中，Phase指的是内存操作的时序阶段，尤其是在内存的读取和写入操作中。例如，在DDR内存的读取操作中，数据可能在时钟信号的特定相位被采样
- [x] 波形是 双phase，FPGA 是单 phase；
	- DFI 中cs有效位是两 bit ，fpga 是单bit；
- [x] CS最终方案：按照对齐方式处理，只要有 DFI 命令产生，cs 就会有一位或者多位为低电平，无论是 cs 高两位还是低两位，都会被映射到 slot0 处理。因此只要有命令，FPGA cs 就是`8'b0000_0011`.
## 下午
- [?] 读写时序无法保证满足 dfi 时序参数
	- 无法保证 FPGA 数据周期还有处理时序周期；
	
- [x] 在 DDRC_TO_PHY_TOP 中添加 FPGA IP 实例化模块；
	- [x] 代码实例化；
	- [x] 接口信号处理；
	- [x] 宏定义控制；
	- [x] 可以编译；
	- [-] 待上板测试；
- [x] 读操作代码更改：
	- [x] 通过编译；
	- [-] 仿真环境构建测试用例麻烦，待上板测试；

## 晚上
- [!] 对齐DFI波形：![[Pasted image 20241111195040.png]]


# `fas:Question`遗留问题
1. MRS 命令有的在DDR上显示，单DFI 无变化。为什么？
2. DFI 上如何区别 DDR 不同 rank 呢？----是不是根据高位地址选取不同 rank ？

# `fas:Delicious`

| CMD   | CS[3:0]     | Description |
| ----- | ----------- | ----------- |
| MRS   | 4'b0011     |             |
| ZQ    | 4'b0011<br> |             |
| REF   | 4'b0011<br> |             |
| ACT   | 4'b1110<br> |             |
| WR/RD | 4'b1110<br> |             |
- cs 信号总结：
	- MRS：`dfi_cs[3:0]=4'b0011`
		- ![[Pasted image 20241111092124.png]]
	- ZQ：
	- REF：
		- 最初会刷新两个 phase；
		- 后续会出现单个 phase 刷新；
	- ACT：单个 bit 变化
		- ![[Pasted image 20241111093103.png]]
	- WR：单 bit 变化；
	- RD：单 bit 变化；

1. Bank 地址是 DDR 存储阵列；
2. 实际操作中，Bank 地址和相应的行地址是同步发出的：行激活；
	1. 行激活之后，将发送列地址寻址命令以及具体的操作指令，同时发送：读写命令代替列寻址；
# `ris:Emotion`
