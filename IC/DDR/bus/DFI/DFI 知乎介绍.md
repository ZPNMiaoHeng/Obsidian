# 3. DFI协议功能 - DFI PHY 与DFI时钟频率比
- 参考DFI v4.0 4.9节
- DFI 时钟

## 3.1 DFI 时钟架构
| Name          | Description                                                  |
| ------------- | ------------------------------------------------------------ |
| DFI  PHY 时钟频率 | PHY 内部时钟，                                                    |
| DFI 时钟频率      | 1. 又或者称`MC时钟频率`；<br>2. **DFI 接口上所有信号**的时钟，**MC核心逻辑**工作在该时钟下； |
-  简单、典型的 `MC-PHY DFI`架构时钟框图：![[Pasted image 20241017091434.png]]
	- 在许多实现中 `PHY` 内部 `CLK` 和 `DRAM` 的时钟（`CK`）频率相同。
	- **PHY CLK** ：在传输数据与控制信号给 DRAM 的同时，`PHY` 也将 `PHY CLK` 通过 **CK 差分对**传输给 `DRAM`，作为 `DRAM` 内部的工作时钟。
- 多倍频的 `MC-PHY DFI`架构时钟框图：
![[Pasted image 20241018094945.png]]

## 3.2 DFI PHY 时钟与DFI时钟频率比
- ![[Pasted image 20241018094004.png]]
## 3.3 频率比优劣势
- 多倍频原理：`MC` 部分的逻辑要比 `PHY` 部分复杂一些，因此 MC 的时钟频率很难做的和 PHY 一样高。
- [ ] 查看协议：其视角是从 DFI PHY 时钟频率出发，对比查看；--mh 2024年10月18日10:20:48
- ![[Pasted image 20241018100318.png]]
	- EX 频率比为 1:4：
		- DRAM 位宽为 32bits ，在一个 DFI PHY 时钟下，可以传输 64bits。
		- DFI PHY 时钟频率变为 4 倍，就可以实现在 DFI 时钟下对数据采样 4 次。
		- 因此 DFI 信号位宽需要变为 4 倍（变为 4 个 phase）。
	- [ ] phase 与 PHY only Interface command slot 有什么不同呢？--mh：2024年10月18日10:20:38
## 3.4 DFI多倍速率时钟定义
多倍速率的 DFI PHY CLK 和 DFI CLK 满足：
1. DFI PHY CLK 和 DFI CLK **相位对齐**；
2. DFI PHY CLK 的频率是 DFI CLK 的**两倍**或者**四倍**；
- ![[Pasted image 20241018103428.png]]
	- 而为 MC 的信号采样**建立时间**考虑，DFI 协议规定 PHY 只能在 DFI PHY CLK 的 **P0 上升沿**发送信号。

## 3.5 多相位地址控制信号行为
![[Pasted image 20241017095516.png]]
- Q0：图中两个时钟相位没有对其；
	- 是否为DFI PHY时钟建立时间？同相位时数据可能会出预约变化状态，直接采样会猜中亚稳态。
	- 根据建立时间约束，在DFI时钟到来前，数据要处于稳定状态；那 DFI PHY采样数据
- Q1：图中为只在P0时刻发送命令，那其余时刻怎么发送呢？
	- P0和P1如何同时发送命令呢？
		- MC有p0一组接口、p1一组接口；
![[Pasted image 20241017100130.png]]

## 6.多相位写数据信号行为
![[Pasted image 20241017101455.png]]
- 图中为了保证DRAM数据带宽（时间✖️数据）大小不变，在降低MC时钟频率后，就需要提高数据带宽；
	- 通过增加数据接口量，单个DFI 接口信号位宽为`64bit`，就增加接口数量；

![[Pasted image 20241017105958.png]]
- Q0：D1怎么在那个时刻采样到数据呢？按照图中时序关系，应该在D0之前才可以采到数据。
	- A0：注意：wrdata和PHY时钟不是同相位的！

### 6.3 DFI 时序参数
详细参考知乎，介绍很详细；


# 参考
- [DDR 学习时间 (Part C - 1)：DFI 协议简介、演进和协议下载 - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/668915189)
