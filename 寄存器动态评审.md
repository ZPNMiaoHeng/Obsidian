#PS80256 

不支持动态配置
1. vcs_host_id：本 switch 内 vcs 绑定的 host id 编号；
2. vcs_nte_spid：本 switch 内 nte 绑定的 spid id 编号；
3. iommu_cpl_original_process：控制未命中 NTB Bus cpl 报文方案是否切换；
4. nt_num_of_vcs_valid、nt_num_of_vcs：VCS 对应 NTE 编码，NT 组播时使用；
5. RW ECO 寄存器不支持动态配置；


PKT
1. ECO
2. 模式切换，等报文是否发完；这里已经做了软件约束，只能写 0 ，进行强制结束发包；![[3-寄存器动态评审.png]]
3. ![[1-寄存器动态评审.png]]
	1. 软复位不支持动态配置；
	2. ecc_sel 用于注错，不支持动态配；
4. 寄存器 reserve 字段修改为不支持 ![[2-寄存器动态评审.png]]
5. ECO 修改为不支持动态配；