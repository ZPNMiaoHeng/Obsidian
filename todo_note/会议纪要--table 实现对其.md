#7000N #会议纪要 

# 结论
- [ ] URPF 全局配置、动态配置，开启后规格减半；--注意动态切换，保证别挂死；
- [ ] L3_ECMP_A 实例化在 L3、ICAP内，csr 如何读写？--- CSR 组播配置，csr 组播配置存在串链物理延迟，功能实现对延迟可接受；


# 遗留问题
- [ ] L3_entry_* _ multicast：找软件对齐使用场景--魏援
	- [ ] 比如：查三次会限制流量三分之一，--内部实现，不对客户体现限制；
	- [ ] bank 内是否会有混合配置？--硬件实现希望内部功能划分 bank 粒度划分，软件同事确定友商单播规格；---软件魏援、秦佳；
	- [ ] 测试需求：8120，组播报文复制一份、打满，确保出现报文查多次表才能查到组播号场景；--测试，陈然；
- [ ] ==ing_l3_next_hop：IM 重定向问题==；
	- [ ] ING_DVP_TABLE：ICAP 重定向；
	- [ ] l3_dlb_flowset_t：DLB 是否需要做 2 次？-- 协议
		- [ ] -- ICAP、L3 中两次 DLB 是否可以合并为一次，在 ICAP 实现呢？如果可以的话，会有什么影响？实现可不可以接受呢？
		- [ ] L3 做标记、ICAP 做标记，在后续实现一次查找 DLB；
- [ ] SRAM 类型改为 spram；
- [ ] ==mpls_entry_t==：
	- [ ] nov3 两次是否有可以合并一次；--全朝辉；
	- [ ] 查两次，会导致性能减半，实现待定（协议、软件需求想要查询多次）；
	- [ ] 8002 MPLS 性能问题：全 MPLS，--陈然
		- [ ] 目前 8002 引出 20 个口 10 CE、10P ；
		- [ ] 支持两层、三层；
		- [ ] 整机转发；
	- [ ] 8120 MPLS 测试；--陈然
- [ ] VLAN_XLATE_t：精确查询、模糊插线开启后，是否会有性能损失；--测试，8120开启后是否约束；
- [x] IFIT_Record_tx_t：1R1W 实现方案；---魏鹏飞 ✅ 2026-07-09


根据笔记内容，我为你优化重构了“会议纪要--table 实现对其”的描述，使结构更清晰，逻辑更严谨。

---

# 会议纪要：Table 实现方案对齐

## 一、 会议结论
- **URPF 策略**：支持全局配置与动态配置。开启后，表项规格减半。
  - **同步风险**：重点关注配置动态切换的逻辑，防止出现切换期间业务挂死。
- **L3_ECMP_A 实例化**：该表需分别在 L3 模块和 ICAP 模块中实例化。
  - **CSR 访问方案**：采用 CSR 组播配置方式实现读写。虽有串链物理延迟，但功能上完全可接受。

## 二、 遗留问题

- **ING_L3_NEXT_HOP 与 DLB 处理**
  - **ING_DVP_TABLE** 重定向在 ICAP 中实现。
  - **DLB 合并可行性**：需评估 ICAP 与 L3 中的两次 DLB 是否可合并为 ICAP 内一次。需分析此方案的影响和实现是否可接受。
  - **备选实现**：在 L3 和 ICAP 分别做标记，后续再实现一次 DLB 查找。
- **SRAM 类型调整**：拟将类型修改为 SPRAM。
- **MPLS 查询性能**
  - **需求分析**：目前协议和软件需求倾向查询多次，但硬件上，查询两次将导致性能减半，整体方案待定。
  - **设计与验证**：需权衡设计实现与性能诉求；分析 8002、8120 等芯片的 MPLS 性能，考量其对多层标签处理、整机转发的实际影响和测试情况。
- **VLAN_XLATE 模糊插线**：评估精确查询、模糊插线功能开启后，是否会有性能损失，需确认 8120 开启后是否有约束。
- **IFIT_Record_tx_t 实现**：1R1W 实现方案已确认。
- **交互与协作待办**
  - 明确 L3 entry multicast 的软件对齐和使用场景。
  - 全朝辉推进 MPLS Nov3 两次查询合并的可能性。
  - 陈然跟进 8002、8120 相关功能的测试与性能反馈。
  - 协议侧确认 DLB 重复执行的诉求。
  - 魏鹏飞负责 1R1W 的实现。

---

**优化要点说明**：
- **结论与遗留问题分层**：将“结论”和“遗留问题”清晰分离，便于快速了解会议成果和待办事项。
- **遗留问题结构化描述**：将原清单中的细节，按照技术主题重新归纳和分组，使每个问题点的讨论逻辑更连贯，方便责任人跟踪。
- **决策与待探索并重**：对已有结论的内容强化约束信息（如风险提醒），对尚待讨论的内容使用“评估”、“倾向”、“拟”等措辞，准确表达当前状态。